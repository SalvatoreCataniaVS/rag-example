# ===========================
# HTTP
# ===========================
quarkus.http.port=8080
quarkus.http.root-path=/api

# ===========================
# DATASOURCE - PostgreSQL (dev + prod)
# ===========================
%dev.quarkus.datasource.db-kind=postgresql
%dev.quarkus.datasource.username=user
%dev.quarkus.datasource.password=password
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5433/ragdb
quarkus.datasource.jdbc.max-size=16
quarkus.datasource.jdbc.min-size=2

# ===========================
# HIBERNATE ORM
# ===========================
%dev.quarkus.hibernate-orm.database.generation=drop-and-create
%dev.quarkus.hibernate-orm.log.sql=true
%dev.quarkus.hibernate-orm.log.format-sql=true

%prod.quarkus.hibernate-orm.database.generation=validate
%prod.quarkus.hibernate-orm.log.sql=false

# ===========================
# SECURITY - Basic Auth (dev ? hardcoded users)
# ===========================
%dev.quarkus.http.auth.basic=true
%dev.quarkus.security.users.embedded.enabled=true
%dev.quarkus.security.users.embedded.plain-text=true

# User: admin / password: admin123 / role: admin
%dev.quarkus.security.users.embedded.users.admin=admin123
%dev.quarkus.security.users.embedded.roles.admin=admin

# User: user / password: user123 / role: user
%dev.quarkus.security.users.embedded.users.user=user123
%dev.quarkus.security.users.embedded.roles.user=user

# Prod: OAuth/OIDC (uncomment when ready)
# %prod.quarkus.oidc.auth-server-url=...
# %prod.quarkus.oidc.client-id=...

# ===========================
# AWS S3 / LocalStack
# ===========================
%dev.s3.endpoint=http://localhost:4566
%dev.s3.region=eu-west-1
%dev.s3.access-key=test
%dev.s3.secret-key=test
%dev.s3.bucket=rag-documents

%prod.s3.endpoint=${S3_ENDPOINT:}
%prod.s3.region=${S3_REGION:eu-west-1}
%prod.s3.access-key=${S3_ACCESS_KEY}
%prod.s3.secret-key=${S3_SECRET_KEY}
%prod.s3.bucket=${S3_BUCKET:rag-documents}

# ===========================
# KAFKA
# ===========================
%dev.kafka.bootstrap.servers=localhost:9092
%prod.kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# ===========================
# KAFKA - Ingestion
# ===========================

# Outgoing ? DocumentService produces events
mp.messaging.outgoing.document-ingestion-out.connector=smallrye-kafka
mp.messaging.outgoing.document-ingestion-out.topic=document-ingestion
mp.messaging.outgoing.document-ingestion-out.value.serializer=org.apache.kafka.common.serialization.StringSerializer

# Incoming ? IngestionConsumer listens for events
mp.messaging.incoming.document-ingestion-in.connector=smallrye-kafka
mp.messaging.incoming.document-ingestion-in.topic=document-ingestion
mp.messaging.incoming.document-ingestion-in.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming.document-ingestion-in.group.id=rag-ingestion-group
mp.messaging.incoming.document-ingestion-in.auto.offset.reset=earliest

# ===========================
# LOG
# ===========================
quarkus.log.level=INFO
%dev.quarkus.log.level=DEBUG
quarkus.log.console.enable=true
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] %s%e%n

# Enable LangChain4j HTTP debug logging
quarkus.log.category."dev.langchain4j".level=DEBUG